{
  "name": "luviio-backend",
  "version": "1.0.0",
  "description": "Luviio FastAPI Backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --port 8000",
    "build": "echo 'Backend build complete'",
    "start": "uvicorn main:app --port 8000"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
